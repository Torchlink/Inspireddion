export const GalleryDisplay = ({ mediaContent }) => {
    return (
      <div>
        {mediaContent.gallery_data && (
          <div className="postGalleryContainer">

          </div>
        )}
      </div>
    );
  };